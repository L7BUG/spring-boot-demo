spring:
    datasource:
        url: jdbc:postgresql://localhost:17000/one-coupon
        username: admin
        password: 123456
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
        properties:
            hibernate:
                default_schema: public
                format_sql: true